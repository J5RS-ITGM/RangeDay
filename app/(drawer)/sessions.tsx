import React from 'react';
import { ComingSoon, Screen, SectionTitle } from '@/components/UI';

export default function Sessions() {
  return (
    <Screen>
      <SectionTitle>Range Sessions</SectionTitle>
      <ComingSoon milestone="M3 — Sessions & analytics" note="Pre-loaded drill plans: build at home, hit Start at the range, and the drills queue up." />
    </Screen>
  );
}
