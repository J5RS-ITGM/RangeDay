import React from 'react';
import { ComingSoon, Screen, SectionTitle } from '@/components/UI';

export default function Drills() {
  return (
    <Screen>
      <SectionTitle>Drills & Stages</SectionTitle>
      <ComingSoon milestone="M2 — Core loop" note="Drill library with search, sort, and filters, plus the community catalog in M5." />
    </Screen>
  );
}
